namespace Infrastructure.Common
{
    public static class ClaimPrincipleExtension
    {
        //public static bool HasClaim(this RazorPageBase razorPage, string claimValue)
        //{
        //    return razorPage.User.HasClaim(e => e.Value == claimValue);
        //}
    }
}
